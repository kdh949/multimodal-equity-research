{
  "schema_version": 1,
  "triggering_command": "python3 -m pytest",
  "warnings": [
    {
      "warning_class": "UserWarning",
      "message_regex": ".*X does not have valid feature names.*LGBMRegressor was fitted with feature names.*",
      "source_path": ".venv/lib/python3.12/site-packages/sklearn/utils/validation.py:2749",
      "accepted_count": 12,
      "rationale": "Optional LightGBM dependency-surface warning from test prediction paths; no signal, validation, model, backtest, or report semantics are changed.",
      "drift_condition": "Fail if class, message regex, source path, command, or count changes without updating this baseline."
    }
  ]
}
